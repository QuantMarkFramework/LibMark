from setuptools import find_packages, setup

setup (
	name='quantmark',
	packages=find_packages(include=['quantmark']),
	version='0.0.1',
	description='test',
	license='MIT',
	test_suite='tests'
)
